[project]
name = "friend-agent-adk"
version = "0.1.0"
description = "A friend agent that uses the Google Agent Development Kit (ADK) to schedule pickleball games."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk>=0.2.5",
    "google-adk>=1.2.1",
    "python-dotenv",
    "uvicorn",
]
