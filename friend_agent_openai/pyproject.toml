[project]
name = "friend-agent-openai"
version = "0.1.0"
description = "A friend agent that uses the OpenAI Assistants API to schedule pickleball games."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk>=0.2.5",
    "openai-agents>=0.4.1",
]
